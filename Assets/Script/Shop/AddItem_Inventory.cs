using UnityEngine;

public class AddItem_Inventory : MonoBehaviour
{
    public GameObject Slot;
    public InventoryManager inventoryManager;

    // Start is called before the first frame update
    void Start() { }

    // Update is called once per frame
    void Update() { }
}
